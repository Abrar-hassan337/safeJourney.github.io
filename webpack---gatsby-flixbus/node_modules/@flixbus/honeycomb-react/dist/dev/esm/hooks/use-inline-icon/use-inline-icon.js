import React from 'react';
import Icon from '../../components/icon/icon.js';
/**
 * Creates an Icon using the provided InlineIcon according to active state.
 * @param InlineIcon the icon or icons to be used.
 * @param {boolean} active the active flag to choose an icon.
 * @returns {HTMLElement} The DOM node to use as the Portal target.
 */

var useInlineIcon = function useInlineIcon(InlineIcon, active) {
    if (InlineIcon) {
        // eslint-disable-next-line no-nested-ternary
        var computedIcon = typeof InlineIcon === 'function' ? InlineIcon : active ? InlineIcon.active : InlineIcon.regular;
        return /*#__PURE__*/ React.createElement(Icon, {
            InlineIcon: computedIcon,
            "aria-hidden": "true"
        });
    }

    return null;
};

export default useInlineIcon;